/**
 * MyMath is my library of customized math functions making use of the 
 * standard Math library.
 * 
 * This is an opportunity to practice writing and calling methods, as well
 * as a chance to review some math methods and properties.
 * 
 * @author (your name)
 * @version (date)
 */

public class MyMathLib
{
    /**
     * Finds the square root of a number.  If the number is negative it returns -1.
     * 
     * @param   num     The number to take the square root of.
     * @return  The square root of num or -1 if num is negative.
     */
    public static double doSqrt( double num )
    {
        return 0;  //This is a placeholder so the program compiles.
    }
    
    // Add the rest of the required methods here.
}